import { MitadMayusculasPipe } from './mitad-mayusculas.pipe';

describe('MitadMayusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new MitadMayusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
