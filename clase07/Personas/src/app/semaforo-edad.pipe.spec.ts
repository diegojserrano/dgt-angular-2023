import { SemaforoEdadPipe } from './semaforo-edad.pipe';

describe('SemaforoEdadPipe', () => {
  it('create an instance', () => {
    const pipe = new SemaforoEdadPipe();
    expect(pipe).toBeTruthy();
  });
});
